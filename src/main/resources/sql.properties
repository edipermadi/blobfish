SQL_CREATE_TABLE_RECIPIENTS=CREATE TABLE IF NOT EXISTS `recipients` (`id` INT PRIMARY KEY, `uuid` VARCHAR(36) NOT NULL, `name` VARCHAR(64) NOT NULL, `meta` CLOB NOT NULL, `certificate` BLOB NOT NULL, `imported` BOOLEAN, `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP);